{
  "name": "github-lines-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API returning GitHub coding stats by username, with week/month diffs in KV.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
